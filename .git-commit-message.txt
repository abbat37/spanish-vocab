feat: Implement Tailwind CSS with professional UI redesign

## Major Changes

### Frontend Redesign
- Replace custom CSS with Tailwind CSS 3.3.2 (utility-first framework)
- Implement calm, professional color palette for language learning
- Add responsive mobile-first design with hamburger menu
- Create custom color themes (primary indigo, calm slate)
- Reduce CSS bundle size from 300KB+ (CDN) to 17KB (optimized build)

### UI/UX Improvements
- **Mobile Navigation**: Collapsible hamburger menu on small screens
- **Dashboard Layout**: 2-row layout (total stats + theme breakdown), themes hidden on mobile
- **Auto-scroll**: Automatically scroll to sentences after generation on mobile
- **Loading States**: Spinner animation on "Generate Sentences" button
- **Warning Colors**: Amber styling for rate-limit warnings (was green)
- **Calmer Aesthetics**: Softer background gradients and highlight colors

### Build Process
- Add npm build tooling for Tailwind CSS compilation
- Configure tree-shaking to include only used CSS classes
- Create development and production build scripts
- Add watch mode for automatic CSS rebuilds during development

### Project Organization
- Move legacy SQL migration to `docs/legacy/`
- Move Tailwind documentation to `docs/TAILWIND_BUILD.md`
- Add `app/static/js/mobile-menu.js` for navigation
- Create `app/static/css/input.css` as Tailwind source file
- Generate `app/static/css/output.css` (gitignored, build artifact)

### Backend Enhancements
- Ensure all 4 themes always display in stats (even with 0 words)
- Update `main.py` to populate theme stats with zeros when empty

## Files Changed

### Added
- `package.json` - Node.js dependencies and build scripts
- `tailwind.config.js` - Tailwind configuration with custom colors
- `app/static/css/input.css` - Tailwind source file
- `app/static/js/mobile-menu.js` - Mobile navigation handler
- `docs/TAILWIND_BUILD.md` - Comprehensive Tailwind documentation
- `docs/legacy/migrate_add_user_auth.sql` - Archived SQL migration

### Modified
- `README.md` - Added Tailwind setup instructions and updated tech stack
- `app/templates/*.html` - Replaced inline styles with Tailwind classes
- `app/routes/main.py` - Theme stats always show all 4 themes
- `.gitignore` - Added Node.js and build artifacts

### Removed
- Custom CSS (replaced by Tailwind)
- CDN script tags (replaced by compiled CSS)
- Bright color schemes (replaced by calm palette)

## Technical Details

**Build Commands:**
- `npm run build:css` - Development build
- `npm run watch:css` - Auto-rebuild on changes
- `npm run build:css:prod` - Minified production build

**Color Palette:**
- Primary: Indigo (#6366f1 to #312e81)
- Calm: Slate (#f8fafc to #0f172a)
- Highlights: Amber (#fef3c7) instead of bright yellow

**Responsive Breakpoints:**
- Mobile: < 640px
- Tablet: 640px - 1023px
- Desktop: 1024px+

## Testing

- Verified on development server
- All pages render correctly
- Mobile menu functions properly
- CSS builds successfully
- Dashboard responsive layout works

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
